/* eslint-disable */
import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

// import '' from '';

const Comp = ({ }) => {

  return (
  <Wrapper>boilerplate return</Wrapper>
  );
};

Comp.propTypes = {
};

export default Comp;


const Wrapper = styled.div`
  order: 1;
  width: 100%;
  max-width: 1280px;
  display: block;
  clear: both;
  margin: 16px 0 0;
`;